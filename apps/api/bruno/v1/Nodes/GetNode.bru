meta {
  name: Get Node
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/nodes/:nodeId
  body: none
  auth: none
}

params:path {
  nodeId: {{nodeId}}
}

docs {
  # Get Node

  Flat address (`nodeId` is globally unique) — doesn't need a workflow/version in the URL.
  `{{nodeId}}` is auto-filled by **Create Node**.
}
