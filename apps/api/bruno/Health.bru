meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

docs {
  # Health

  Always `200 { "status": "ok" }` if the process is alive — never touches the database.
}
