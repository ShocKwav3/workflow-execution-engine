# Routes

Whenever an HTTP route is added, changed, or removed here, update the Bruno collection under `apps/api/bruno/` in the same change — not as a follow-up. See `globalAgentInstructions.md` §20.
